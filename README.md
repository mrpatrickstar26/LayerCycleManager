
---

# Layer Cycle Manager (LCM)

> Нативная программа для AutoCAD, предназначенная для автоматического построения стрелок между точками, расположенными на разных слоях чертежа.  
> Программа является портом исходной Python-разработки `Layer Cycle Manager` на технологии AutoCAD: **AutoLISP / Visual LISP + ActiveX / COM**, с natивным DCL-интерфейсом и дальнейшей интеграцией через **CUIX**.

---

## 1. Назначение программы

Программа используется для визуализации связей между одноимёнными или вручную сопоставленными точками разных циклов расчёта.

Основная задача:

- выбрать слой с точками нулевого цикла;
- выбрать слой с точками последнего цикла;
- сопоставить точки между слоями автоматически или вручную;
- построить стрелки от точек нулевого слоя к точкам последнего слоя;
- подписать длину исходного отрезка в миллиметрах;
- разместить стрелки на целевом слое;
- назначить цвет:
  - по слою;
  - по индексу ACI;
  - по RGB.

Программа разрабатывается как автономный инструмент для AutoCAD, который в дальнейшем можно будет устанавливать по аналогии с menuGEO:

- папка с файлами;
- загрузка интерфейса через `CUIX`;
- без установки Python и внешних библиотек.

---

## 2. Текущий статус проекта

| Этап | Описание | Статус |
|---|---|---|
| Этап 0 | Фиксация требований, подготовка тестового чертежа | Выполнено |
| Этап 1 | Настройка среды разработки, VSCode, Git, GitHub | Выполнено |
| Этап 2 | Каркас программы, файл `lcm_main.lsp`, команда `LCM` | Выполнено |
| Этап 3 | Чтение данных чертежа и консольная отрисовка стрелок | Завершено, подтверждено |
| Этап 4 | DCL-интерфейс | Завершено, подтверждено |
| Этап 5 | CUIX-интеграция в AutoCAD | Не начат |
| Этап 6 | Тестирование | Частично выполняется на этапах 3 и 4 |
| Этап 7 | Упаковка и автономная установка | Не начат |

---

## 3. Подтверждённые результаты Этапа 3

По состоянию на текущую версию подтверждено, что работает:

1. Чтение списка слоёв чертежа.
2. Чтение точек и текстов с выбранного слоя.
3. Сопоставление точки с ближайшим текстом.
4. Формирование имени точки:
   - если текст найден — имя берётся из текста;
   - если текст не найден — используется префикс:
     ```text
     Point_
     ```
     и handle точки.
5. Сортировка имён точек:
   - числовые имена сортируются по числовому значению;
   - строковые имена сортируются лексикографически.
6. Ручная отрисовка тестовой стрелки.
7. Автоматическая отрисовка стрелок по одинаковым именам.
8. Длина стрелки считается только по координатам `X` и `Y`.
9. Длина подписывается в миллиметрах:
   ```text
   длина в мм = длина в метрах × 1000
   ```
10. Округление длины выполняется по математическим правилам.
11. Масштаб влияет только на визуальное удлинение стрелки.
12. Текст длины не зависит от масштаба.
13. Наконечник стрелки строится как залитый треугольник `AcDbSolid`.
14. Все объекты стрелки размещаются на целевом слое.
15. Цвет стрелок берётся из целевого слоя через режим:
   ```lisp
   "BYLAYER"
   ```
16. Пакетная отрисовка обёрнута в один undo-блок, поэтому результат можно отменить одним `Ctrl + Z`.

---

## 4. Подтверждённые результаты Этапа 4

По состоянию на текущую версию подтверждено, что работает:

1. Команда `LCM` открывает нативный DCL-диалог AutoCAD.
2. Диалог содержит выбор нулевого и последнего слоёв.
3. Список слоёв заполняется автоматически при открытии диалога.
4. Кнопка «Обновить слои» обновляет список слоёв из текущего чертежа.
5. При выборе слоёв программа читает точки с соответствующих слоёв.
6. Информационная строка показывает количество найденных точек на нулевом и последнем слоях.
7. Реализован выбор режима цвета:
   - «По слою»;
   - `ACI`;
   - `RGB`.
8. Режим «По слою» использует:
   ```lisp
   "BYLAYER"
   ```
   и стрелки наследуют цвет целевого слоя.
9. Режим `ACI` позволяет задать цвет по индексу от 1 до 255.
10. Режим `RGB` позволяет задать TrueColor-цвет вручную через значения R, G, B.
11. Поля, не относящиеся к выбранному режиму цвета, блокируются.
12. Реализована настройка высоты текста.
13. Реализована настройка масштаба стрелок.
14. Реализован список сопоставлений точек.
15. Реализовано автоматическое сопоставление точек по одинаковым именам.
16. Если точка с таким же именем не найдена, создаётся запись с placeholder:
   ```text
   <not found>
   ```
17. Реализовано ручное добавление сопоставлений через выпадающие списки.
18. Реализовано обновление выбранного сопоставления.
19. Реализовано удаление выбранного сопоставления.
20. Реализована очистка всех сопоставлений.
21. Кнопка «Выполнить» проверяет ввод и запускает отрисовку стрелок.
22. Некорректные сопоставления пропускаются.
23. После нажатия «Выполнить» диалог закрывается, а стрелки строятся в AutoCAD.
24. Кнопка «Закрыть» закрывает диалог без выполнения отрисовки.

---

## 5. Стек проекта

| Технология | Назначение | Статус |
|---|---|---|
| AutoCAD 2018+ | Среда выполнения | Используется |
| AutoLISP | Основной язык логики | Используется |
| Visual LISP | Расширенные функции AutoLISP | Используется |
| ActiveX / COM | Работа с объектами AutoCAD | Используется |
| DCL | Нативные диалоги AutoCAD | Реализовано |
| CUIX | Интеграция в Ribbon / интерфейс AutoCAD | Планируется |
| VSCode | Среда разработки | Используется |
| Git | Версионирование | Используется |
| GitHub | Удалённый репозиторий | Используется |
| Python / pyautocad | Исходный прототип | Больше не используется в ядре программы |

---

## 6. Архитектура проекта

Программа разделена на модули:

```text
LayerCycleManager/
│
├── README.md               ; документация проекта
├── .gitignore              ; файлы и папки, исключаемые из Git
│
├── lcm_main.lsp            ; точка входа, команды LCM и LCMRELOAD
├── lcm_data.lsp            ; работа с данными чертежа
├── lcm_core.lsp            ; геометрия и отрисовка стрелок
├── lcm_config.lsp          ; будущие настройки программы
├── lcm_ui.lsp              ; логика DCL-интерфейса
├── lcm_ui.dcl              ; описание DCL-диалога
│
├── icons/                  ; будущие иконки для CUIX
│   ├── lcm_16.bmp
│   └── lcm_32.bmp
│
├── settings/               ; будущие локальные настройки программы
│   └── lcm_settings.cfg
│
└── loader/                 ; будущий вспомогательный загрузчик
    └── lcm_loader.lsp
```

---

## 7. Описание файлов проекта

### 7.1. `README.md`

Документация проекта.

Содержит:

- назначение программы;
- стек технологий;
- структуру файлов;
- описание команд;
- описание функций;
- текущий статус разработки;
- порядок запуска;
- известные ограничения;
- план развития.

---

### 7.2. `.gitignore`

Служебный файл для системы контроля версий Git.

Определяет, какие файлы не нужно отправлять в GitHub.

Например:

- временные файлы AutoCAD;
- резервные копии;
- локальные настройки программы;
- локальные настройки VSCode;
- кэш Python, если в проекте остаются файлы прототипа.

---

### 7.3. `lcm_main.lsp`

Главный файл программы.

Отвечает за:

- инициализацию Visual LISP COM;
- глобальные переменные проекта;
- загрузку модулей:
  - `lcm_config.lsp`;
  - `lcm_data.lsp`;
  - `lcm_core.lsp`;
  - `lcm_ui.lsp`;
- команду запуска:
  ```text
  LCM
  ```
- вспомогательную команду перезагрузки модулей:
  ```text
  LCMRELOAD
  ```

#### Основные сущности файла

| Имя | Тип | Назначение |
|---|---|---|
| `lcm:version` | переменная | версия программы |
| `lcm:debug` | переменная | режим отладочных сообщений |
| `lcm:ui-loaded` | переменная | флаг готовности интерфейса |
| `lcm:load-module` | функция | безопасная загрузка одного модуля |
| `lcm:load-modules` | функция | загрузка всех модулей проекта |
| `lcm:start` | функция | точка запуска программы |
| `C:LCM` | команда AutoCAD | основная команда программы |
| `C:LCMRELOAD` | команда AutoCAD | перезагрузка модулей |

---

### 7.4. `lcm_data.lsp`

Модуль работы с данными чертежа.

Отвечает за:

- получение списка слоёв;
- поиск точек на указанном слое;
- поиск текстов и мультитекстов на указанном слое;
- сопоставление каждой точки с ближайшим текстом;
- формирование имени точки;
- сортировку имён точек;
- автоматическое сопоставление точек двух слоёв.

#### Основные сущности файла

| Имя | Тип | Назначение |
|---|---|---|
| `lcm:text-search-radius` | переменная | радиус поиска текста около точки |
| `lcm:unnamed-point-prefix` | переменная | префикс для точек без текста |
| `lcm:get-layers` | функция | получить список слоёв чертежа |
| `lcm:get-points-with-names` | функция | получить точки слоя с именами |
| `lcm:get-object-coords` | функция | получить координаты объекта |
| `lcm:get-object-handle` | функция | получить handle объекта |
| `lcm:is-numeric` | функция | проверка, является ли имя числом |
| `lcm:name<` | функция | сравнение имён для сортировки |
| `lcm:sort-points` | функция | сортировка списка точек |
| `lcm:add-or-replace-point` | функция | добавление или замена точки в списке |
| `lcm:get-point-names` | функция | получить только имена точек |
| `lcm:auto-mappings` | функция | автоматическое сопоставление точек |
| `C:LCMLAYERS` | команда AutoCAD | вывести список слоёв |
| `C:LCMTESTDATA` | команда AutoCAD | вывести точки и их имена |

---

### 7.5. `lcm_core.lsp`

Модуль геометрии и отрисовки.

Отвечает за:

- математический расчёт стрелки;
- построение линии;
- построение залитого наконечника;
- построение текста длины;
- назначение слоя;
- назначение цвета;
- пакетную отрисовку стрелок;
- тестовые команды.

#### Основные сущности файла

| Имя | Тип | Назначение |
|---|---|---|
| `lcm:deg2rad` | функция | перевод градусов в радианы |
| `lcm:pt3` | функция | преобразование точки в 3D-точку ActiveX |
| `lcm:is-string` | функция | проверка строкового типа |
| `lcm:distance-2d` | функция | расстояние между точками только по X и Y |
| `lcm:ensure-layer` | функция | создание слоя при отсутствии |
| `lcm:set-object-properties` | функция | назначение слоя и цвета объекту |
| `lcm:draw-arrow` | функция | отрисовка одной стрелки |
| `lcm:run` | функция | отрисовка набора стрелок |
| `C:LCMTESTDRAW` | команда AutoCAD | ручная тестовая стрелка |
| `C:LCMTESTRUN` | команда AutoCAD | автоматическая тестовая отрисовка |

---

### 7.6. `lcm_config.lsp`

Файл настроек программы.

На текущем этапе является заготовкой.

В дальнейшем будет отвечать за:

- чтение настроек из файла;
- сохранение настроек;
- последние выбранные слои;
- масштаб;
- высоту текста;
- режим цвета;
- радиус поиска текста;
- коэффициент перевода единиц чертежа в миллиметры.

---

### 7.7. `lcm_ui.lsp`

Модуль пользовательского интерфейса.

Файл реализован и отвечает за:

- открытие DCL-диалога;
- заполнение списков слоёв;
- сохранение выбранных слоёв при обновлении списка;
- чтение точек с нулевого и последнего слоёв;
- обновление информационной строки с количеством точек;
- заполнение выпадающих списков точек для ручного сопоставления;
- ведение списка сопоставлений;
- авто-сопоставление точек;
- ручное добавление сопоставлений;
- обновление выбранного сопоставления;
- удаление выбранного сопоставления;
- очистку всех сопоставлений;
- переключение режимов цвета:
  - ByLayer;
  - ACI;
  - RGB;
- блокировку неиспользуемых полей цвета;
- проверку корректности введённых данных;
- запуск ядра отрисовки после закрытия диалога.

#### Основные сущности файла

| Имя | Тип | Назначение |
|---|---|---|
| `lcm:show-main-dialog` | функция | открывает главный DCL-диалог |
| `lcm:ui-fill-layers` | функция | заполняет списки слоёв |
| `lcm:ui-refresh-points` | функция | читает точки с выбранных слоёв |
| `lcm:ui-fill-point-combos` | функция | заполняет списки точек для сопоставления |
| `lcm:ui-fill-mappings` | функция | обновляет список сопоставлений |
| `lcm:ui-add-map` | функция | добавляет новое сопоставление |
| `lcm:ui-update-map` | функция | обновляет выбранное сопоставление |
| `lcm:ui-remove-map` | функция | удаляет выбранное сопоставление |
| `lcm:ui-auto-map` | функция | выполняет авто-сопоставление |
| `lcm:ui-clear-map` | функция | очищает все сопоставления |
| `lcm:ui-fill-color-mode` | функция | заполняет список режимов цвета |
| `lcm:ui-update-color-mode` | функция | включает/отключает поля цвета |
| `lcm:ui-pick-aci` | функция | открывает выбор цвета ACI, если доступен |
| `lcm:ui-valid-mappings` | функция | возвращает только корректные сопоставления |
| `lcm:ui-accept` | функция | проверяет данные перед выполнением |
| `lcm:ui-execute` | функция | запускает отрисовку после закрытия диалога |
| `lcm:ui-loaded` | переменная | флаг готовности интерфейса |
| `lcm:placeholder` | переменная | текст для ненайденных точек |

---

### 7.8. `lcm_ui.dcl`

Файл пользовательского интерфейса DCL.

Файл реализован и содержит описание главного диалога программы:

- выбор нулевого слоя;
- выбор последнего слоя;
- кнопку обновления слоёв;
- выбор режима цвета;
- поле ввода ACI;
- кнопку выбора цвета ACI;
- поля ввода RGB;
- поле высоты текста;
- поле масштаба;
- информационную строку;
- выпадающий список точек нулевого слоя;
- выпадающий список точек последнего слоя;
- список сопоставлений;
- кнопки:
  - добавить сопоставление;
  - обновить сопоставление;
  - удалить сопоставление;
  - авто-сопоставление;
  - очистить все;
- кнопку «Выполнить»;
- кнопку «Закрыть».

Основной диалог:

```text
lcm_main_dlg
```

Ключевые элементы DCL:

| Key | Назначение |
|---|---|
| `layer_zero` | выбор нулевого слоя |
| `layer_last` | выбор последнего слоя |
| `refresh_layers` | обновление списка слоёв |
| `color_mode` | выбор режима цвета |
| `aci` | ввод цвета ACI |
| `pick_aci` | кнопка выбора цвета ACI |
| `rgb_r` | ввод красного канала RGB |
| `rgb_g` | ввод зелёного канала RGB |
| `rgb_b` | ввод синего канала RGB |
| `text_height` | высота текста |
| `scale` | масштаб стрелок |
| `info` | информационная строка |
| `map_from` | выбор точки из нулевого слоя |
| `map_to` | выбор точки из последнего слоя |
| `mappings` | список сопоставлений |
| `add_map` | добавить сопоставление |
| `update_map` | обновить сопоставление |
| `remove_map` | удалить сопоставление |
| `auto_map` | авто-сопоставление |
| `clear_map` | очистить все сопоставления |
| `accept` | выполнить |
| `cancel` | закрыть |

---

### 7.9. `icons/`

Папка для будущих иконок программы.

Планируются файлы:

```text
lcm_16.bmp
lcm_32.bmp
```

Иконки будут использоваться в `CUIX` для кнопок Ribbon.

---

### 7.10. `settings/`

Папка для будущих локальных настроек программы.

Планируемый файл:

```text
lcm_settings.cfg
```

В него могут записываться:

- последние выбранные слои;
- масштаб;
- высота текста;
- режим цвета;
- дополнительные параметры.

---

### 7.11. `loader/`

Папка для будущего вспомогательного загрузчика.

Планируемый файл:

```text
lcm_loader.lsp
```

Может использоваться для первичной загрузки программы, если папка проекта ещё не добавлена в Support File Search Path.

---

## 8. Используемые команды AutoCAD

### 8.1. Основные команды

| Команда | Назначение |
|---|---|
| `LCM` | Основная команда программы. Открывает главный DCL-диалог программы |
| `LCMRELOAD` | Перезагружает модули `lcm_config.lsp`, `lcm_data.lsp`, `lcm_core.lsp`, `lcm_ui.lsp` |

---

### 8.2. Команды данных

| Команда | Назначение |
|---|---|
| `LCMLAYERS` | Выводит список всех слоёв текущего чертежа |
| `LCMTESTDATA` | Выводит точки и их имена для выбранного слоя |

---

### 8.3. Команды отрисовки

| Команда | Назначение |
|---|---|
| `LCMTESTDRAW` | Рисует одну тестовую стрелку по двум выбранным точкам |
| `LCMTESTRUN` | Автоматически сопоставляет точки двух слоёв и рисует стрелки |

---

## 9. Подробное описание реализованных функций

---

### 9.1. Функции `lcm_main.lsp`

#### `lcm:load-module`

```lisp
(lcm:load-module "lcm_data.lsp")
```

Безопасно загружает модуль.

Действие:

1. Ищет файл через `findfile`.
2. Если файл найден — загружает его.
3. Если файла нет — продолжает работу.
4. При ошибке загрузки выводит сообщение.

---

#### `lcm:load-modules`

```lisp
(lcm:load-modules)
```

Загружает модули проекта в правильном порядке:

```text
lcm_config.lsp
lcm_data.lsp
lcm_core.lsp
lcm_ui.lsp
```

Перед загрузкой сбрасывает флаг:

```lisp
lcm:ui-loaded
```

---

#### `lcm:start`

```lisp
(lcm:start)
```

Главная функция запуска.

Действие:

- проверяет флаг готовности интерфейса;
- если интерфейс готов, вызывает:
  ```lisp
  lcm:show-main-dialog
  ```
- если интерфейс не готов, выводит служебное сообщение.

---

#### `C:LCM`

```lisp
(defun C:LCM ()
  (lcm:start)
  (princ)
)
```

Команда AutoCAD:

```text
LCM
```

---

#### `C:LCMRELOAD`

```lisp
(defun C:LCMRELOAD ()
  (lcm:load-modules)
  (princ)
)
```

Команда AutoCAD:

```text
LCMRELOAD
```

Используется при разработке для перезагрузки модулей без перезапуска AutoCAD.

---

### 9.2. Функции `lcm_data.lsp`

#### `lcm:get-layers`

```lisp
(lcm:get-layers)
```

Возвращает отсортированный список слоёв текущего чертежа.

Используется для заполнения выпадающих списков в интерфейсе.

---

#### `lcm:get-points-with-names`

```lisp
(lcm:get-points-with-names "Уч.4 Цикл 0")
```

Основная функция получения данных со слоя.

Алгоритм:

1. Сканирует объекты Model Space.
2. Отбирает объекты указанного слоя.
3. Собирает:
   - точки `AcDbPoint`;
   - тексты `AcDbText`;
   - мультитексты `AcDbMText`.
4. Для каждой точки ищет ближайший текст.
5. Если текст найден, имя точки равно тексту.
6. Если текст не найден, имя формируется как:

```text
Point_<Handle>
```

7. Возвращает отсортированный список вида:

```lisp
(
  ("1" . (100.0 200.0))
  ("2" . (150.0 220.0))
  ("Point_1A2B" . (180.0 260.0))
)
```

---

#### `lcm:get-object-coords`

```lisp
(lcm:get-object-coords obj)
```

Получает координаты объекта.

Сначала пытается использовать:

```lisp
Coordinates
```

Если не получается, пытается использовать:

```lisp
InsertionPoint
```

Возвращает список:

```lisp
(x y)
```

---

#### `lcm:get-object-handle`

```lisp
(lcm:get-object-handle obj)
```

Возвращает handle объекта.

Используется для формирования имени точки, если рядом нет текста.

---

#### `lcm:is-numeric`

```lisp
(lcm:is-numeric "12")
```

Проверяет, является ли строка числом.

Примеры:

| Значение | Результат |
|---|---|
| `"12"` | `T` |
| `"12.5"` | `T` |
| `"-7"` | `T` |
| `"12a"` | `nil` |
| `"abc"` | `nil` |

---

#### `lcm:name<`

```lisp
(lcm:name< "2" "10")
```

Функция сравнения имён для сортировки.

Правила:

- числовые имена сортируются по числовому значению;
- нечисловые имена сортируются лексикографически;
- числовые имена идут перед нечисловыми.

---

#### `lcm:sort-points`

```lisp
(lcm:sort-points points)
```

Сортирует список точек по имени.

---

#### `lcm:add-or-replace-point`

```lisp
(lcm:add-or-replace-point pairs name coord)
```

Добавляет точку в список.

Если точка с таким именем уже существует, она заменяется новой.

---

#### `lcm:get-point-names`

```lisp
(lcm:get-point-names points)
```

Возвращает только имена точек без координат.

---

#### `lcm:auto-mappings`

```lisp
(lcm:auto-mappings points-zero points-last)
```

Создаёт автоматические сопоставления.

Принцип:

- для каждой точки нулевого слоя ищется точка с таким же именем в последнем слое;
- если совпадение найдено, создаётся пара:

```lisp
((from . "1") (to . "1"))
```

- если совпадение не найдено, пара не создаётся.

---

#### `C:LCMLAYERS`

```text
LCMLAYERS
```

Тестовая команда.

Выводит список слоёв чертежа в командную строку AutoCAD.

---

#### `C:LCMTESTDATA`

```text
LCMTESTDATA
```

Тестовая команда.

Запрашивает имя слоя и выводит найденные точки:

```text
1  X=100.000  Y=200.000
2  X=150.000  Y=220.000
```

---

### 9.3. Функции `lcm_core.lsp`

#### `lcm:deg2rad`

```lisp
(lcm:deg2rad 10.0)
```

Переводит градусы в радианы.

---

#### `lcm:pt3`

```lisp
(lcm:pt3 '(100.0 200.0))
```

Преобразует 2D-точку в 3D-точку ActiveX:

```lisp
(x y 0.0)
```

---

#### `lcm:is-string`

```lisp
(lcm:is-string "text")
```

Проверяет, является ли значение строкой.

Заменяет отсутствующую в AutoLISP функцию `stringp`.

---

#### `lcm:distance-2d`

```lisp
(lcm:distance-2d p1 p2)
```

Вычисляет расстояние между двумя точками только по координатам X и Y.

Координата Z игнорируется.

Это соответствует логике исходного Python-скрипта:

```python
math.hypot(dx, dy)
```

---

#### `lcm:ensure-layer`

```lisp
(lcm:ensure-layer "MyLayer")
```

Проверяет наличие слоя.

Если слой отсутствует, пытается создать его.

---

#### `lcm:set-object-properties`

```lisp
(lcm:set-object-properties obj layer-name color-mode color-data)
```

Назначает объекту:

- слой;
- цвет.

Поддерживаемые режимы цвета:

| Режим | Описание |
|---|---|
| `"BYLAYER"` | цвет берётся из слоя объекта |
| `"ACI"` | цвет по индексу AutoCAD Color Index |
| `"RGB"` | TrueColor RGB |

Для режима:

```lisp
"BYLAYER"
```

устанавливается цвет:

```lisp
256
```

---

#### `lcm:draw-arrow`

```lisp
(lcm:draw-arrow
  start-point
  end-point
  layer-name
  color-mode
  color-data
  text-height
  scale
)
```

Рисует одну стрелку.

Параметры:

| Параметр | Назначение |
|---|---|
| `start-point` | начальная точка |
| `end-point` | конечная точка |
| `layer-name` | слой для объектов стрелки |
| `color-mode` | режим цвета |
| `color-data` | данные цвета |
| `text-height` | высота текста |
| `scale` | масштаб удлинения стрелки |

Геометрия:

- линия рисуется от `start` до:
  ```text
  start + (end - start) * scale
  ```
- длина текста считается по исходным точкам без учёта `scale`;
- длина переводится в миллиметры:
  ```text
  length * 1000
  ```
- округление выполняется по математическим правилам;
- наконечник — залитый треугольник `AcDbSolid`;
- угол при вершине наконечника — 20°;
- длина крыла наконечника:
  ```text
  0.2 * length * scale
  ```

---

#### `lcm:run`

```lisp
(lcm:run
  layer-zero
  layer-last
  mappings
  scale
  color-mode
  color-data
  text-height
)
```

Главная функция пакетной отрисовки.

Действие:

1. Получает точки нулевого слоя.
2. Получает точки последнего слоя.
3. Проверяет наличие точек.
4. Запускает отрисовку для каждого сопоставления.
5. Обёртывает процесс в один undo-блок:
   ```lisp
   vla-StartUndoMark
   vla-EndUndoMark
   ```
6. Выводит количество созданных стрелок.
7. Выводит количество ошибок.

---

#### `C:LCMTESTDRAW`

```text
LCMTESTDRAW
```

Тестовая команда ручной отрисовки.

Порядок работы:

1. Указать начальную точку.
2. Указать конечную точку.
3. Ввести слой для стрелки.
4. Ввести масштаб.

Текущее поведение:

- стрелка создаётся на указанном слое;
- цвет объектов наследуется от указанного слоя;
- используется режим:
  ```lisp
  "BYLAYER"
  ```

---

#### `C:LCMTESTRUN`

```text
LCMTESTRUN
```

Тестовая команда автоматической отрисовки.

Порядок работы:

1. Ввести имя нулевого слоя.
2. Ввести имя последнего слоя.
3. Ввести масштаб.

Действие:

- читает точки с обоих слоёв;
- создаёт автоматические сопоставления по одинаковым именам;
- рисует стрелки на последнем слое;
- цвет стрелок берётся из последнего слоя.

---

### 9.4. Функции `lcm_ui.lsp`

#### `lcm:show-main-dialog`

```lisp
(lcm:show-main-dialog)
```

Главная функция интерфейса.

Действие:

1. Загружает файл:
   ```text
   lcm_ui.dcl
   ```
2. Открывает диалог:
   ```text
   lcm_main_dlg
   ```
3. Заполняет списки слоёв.
4. Читает точки с выбранных слоёв.
5. Заполняет список режимов цвета.
6. Назначает обработчики кнопкам и спискам.
7. Ожидает действий пользователя.
8. После нажатия «Выполнить» закрывает диалог и запускает отрисовку.

---

#### `lcm:ui-fill-layers`

Заполняет выпадающие списки слоёв.

При повторном обновлении старается сохранить ранее выбранные слои.

---

#### `lcm:ui-refresh-points`

Читает точки с выбранного нулевого и последнего слоёв.

Обновляет:

- список точек нулевого слоя;
- список точек последнего слоя;
- информационную строку;
- выпадающие списки для ручного сопоставления;
- отображение списка сопоставлений.

---

#### `lcm:ui-add-map`

Добавляет новое сопоставление.

Если для выбранной исходной точки уже есть сопоставление, старая запись заменяется новой.

---

#### `lcm:ui-update-map`

Обновляет выбранное сопоставление в списке.

Использует текущие значения из выпадающих списков:

```text
map_from
map_to
```

---

#### `lcm:ui-remove-map`

Удаляет выбранное сопоставление из списка.

---

#### `lcm:ui-auto-map`

Выполняет автоматическое сопоставление.

Для каждой точки нулевого слоя:

- если на последнем слое есть точка с таким же именем, создаётся пара:
  ```text
  имя -> имя
  ```
- если такой точки нет, создаётся запись:
  ```text
  имя -> <not found>
  ```

---

#### `lcm:ui-clear-map`

Удаляет все сопоставления из списка.

---

#### `lcm:ui-update-color-mode`

Управляет доступностью полей цвета.

Режимы:

| Режим | Доступные поля |
|---|---|
| По слою | нет цветовых полей |
| ACI | поле ACI и кнопка выбора |
| RGB | поля R, G, B |

---

#### `lcm:ui-valid-mappings`

Возвращает только те сопоставления, которые будут участвовать в отрисовке.

Условия:

- имя исходной точки не пустое;
- имя целевой точки не пустое;
- целевая точка не является placeholder;
- обе точки существуют на выбранных слоях.

---

#### `lcm:ui-accept`

Выполняется при нажатии кнопки «Выполнить».

Проверяет:

- выбраны ли оба слоя;
- корректен ли масштаб;
- корректна ли высота текста;
- корректен ли режим цвета;
- корректны ли значения ACI или RGB;
- есть ли хотя бы одно корректное сопоставление.

Если проверка пройдена, сохраняет параметры в глобальные переменные и закрывает диалог.

---

#### `lcm:ui-execute`

Запускает отрисовку после закрытия диалога.

Использует сохранённые параметры:

```lisp
lcm:run-layer-zero
lcm:run-layer-last
lcm:run-scale
lcm:run-color-mode
lcm:run-color-data
lcm:run-text-height
lcm:run-mappings
```

---

## 10. Форматы данных

### 10.1. Список точек

Функция:

```lisp
(lcm:get-points-with-names layer-name)
```

возвращает список вида:

```lisp
(
  ("1" . (100.0 200.0))
  ("2" . (150.0 220.0))
  ("10" . (180.0 260.0))
  ("Point_1A2B" . (200.0 300.0))
)
```

Где:

- первый элемент — имя точки;
- второй элемент — координаты `(x y)`.

---

### 10.2. Список сопоставлений

Формат сопоставлений:

```lisp
(
  (
    (from . "1")
    (to . "1")
  )
  (
    (from . "2")
    (to . "5")
  )
)
```

Где:

- `from` — имя точки на нулевом слое;
- `to` — имя точки на последнем слое.

---

## 11. Ключевые глобальные переменные

| Переменная | Назначение | Текущее значение |
|---|---|---|
| `lcm:version` | версия программы | `"0.2.1"` |
| `lcm:debug` | вывод служебных сообщений | `T` или `nil` |
| `lcm:ui-loaded` | флаг готовности интерфейса | `nil` или `T` |
| `lcm:text-search-radius` | радиус поиска текста | `50.0` |
| `lcm:unnamed-point-prefix` | префикс для точек без имени | `"Point_"` |
| `lcm:placeholder` | placeholder для ненайденных точек в интерфейсе | `"<not found>"` |

---

## 12. Важные допущения и правила

### 12.1. Единицы чертежа

На текущем этапе предполагается, что чертёж выполнен в метрах.

Длина в тексте стрелки рассчитывается так:

```text
длина в мм = длина в метрах * 1000
```

Округление выполняется до целого числа.

---

### 12.2. Пространство объектов

На текущем этапе программа сканирует:

```text
ModelSpace
```

Объекты в Layout, блоках или XREF пока не обрабатываются.

---

### 12.3. Цвет объектов

Текущая целевая логика:

```text
Цвет стрелки = цвет слоя, на котором создана стрелка
```

Для этого используется режим:

```lisp
"BYLAYER"
```

---

### 12.4. Длина стрелки и масштаб

Длина текста всегда считается по исходным точкам.

Масштаб влияет только на визуальное удлинение линии:

```text
конец линии = start + (end - start) * scale
```

---

## 13. Как запустить программу

### 13.1. Загрузка через Support Path

Если папка проекта добавлена в:

```text
OPTIONS → Files → Support File Search Path
```

выполнить:

```lisp
(load "lcm_main.lsp")
```

Затем можно использовать команды:

```text
LCM
LCMRELOAD
LCMLAYERS
LCMTESTDATA
LCMTESTDRAW
LCMTESTRUN
```

---

### 13.2. Загрузка полным путём

Если папка не добавлена в Support Path:

```lisp
(load "C:/Users/ИмяПользователя/Desktop/LayerCycleManager/lcm_main.lsp")
```

Пример:

```lisp
(load "C:/Users/Durkin.RI/Desktop/LayerCycleManager/lcm_main.lsp")
```

---

## 14. Проверка текущего функционала

### 14.1. Проверка версий и модулей

```text
LCM
```

Ожидаемый результат:

- открывается DCL-диалог программы.

---

### 14.2. Проверка перезагрузки модулей

```text
LCMRELOAD
```

Ожидаемый результат:

```text
LCM: reloading modules...
LCM: loaded module: ...lcm_config.lsp
LCM: loaded module: ...lcm_data.lsp
LCM: loaded module: ...lcm_core.lsp
LCM: loaded module: ...lcm_ui.lsp
LCM: modules reloaded.
```

---

### 14.3. Проверка слоёв

```text
LCMLAYERS
```

Ожидаемый результат:

```text
LCM: layers count: N
список слоёв
```

---

### 14.4. Проверка чтения точек

```text
LCMTESTDATA
```

Ожидаемый результат:

```text
LCM: found N points on layer 'Имя слоя'
имена и координаты точек
```

---

### 14.5. Проверка интерфейса

```text
LCM
```

Ожидаемый результат:

- открывается диалог;
- видны списки слоёв;
- видны параметры стрелок;
- виден блок сопоставлений;
- при выборе слоёв обновляется количество точек;
- работают кнопки сопоставления;
- кнопка «Выполнить» строит стрелки.

---

## 15. Известные ограничения текущей версии

1. Интерфейс реализован на DCL, но имеет ограничения DCL:
   - сопоставления отображаются обычным списком;
   - редактирование выполняется через отдельные выпадающие списки;
   - нет полноценной таблицы с combobox в каждой строке.
2. Настройки программы пока не сохраняются между запусками.
3. Нет CUIX-интеграции.
4. Выбор цвета через системный диалог пока доступен только для ACI и только если функция `acad_colordlg` поддерживается в конкретной версии AutoCAD.
5. RGB вводится вручную через поля R, G, B.
6. Объекты читаются только из Model Space.
7. Радиус поиска текста пока задаётся только переменной:
   ```lisp
   lcm:text-search-radius
   ```
8. Форматирование MText обрабатывается упрощённо.
9. Единицы чертежа пока жёстко считаются метрами.

---

## 16. План развития проекта

### Этап 4. DCL-интерфейс

Этап завершён.

Реализовано:

- выбор слоёв;
- чтение точек;
- авто-сопоставление;
- ручное сопоставление;
- удаление сопоставлений;
- очистка сопоставлений;
- выбор режима цвета;
- высота текста;
- масштаб;
- запуск отрисовки из диалога.

---

### Этап 5. CUIX-интеграция

Будет создан файл:

```text
lcm.cuix
```

Он добавит в AutoCAD:

- вкладку программы;
- панель программы;
- кнопку запуска команды `LCM`.

---

### Этап 6. Настройки

Будет реализован файл:

```text
lcm_config.lsp
```

и, при необходимости:

```text
settings/lcm_settings.cfg
```

Настройки будут сохранять:

- последние выбранные слои;
- масштаб;
- высоту текста;
- режим цвета;
- ACI;
- RGB;
- радиус поиска текста.

---

### Этап 7. Упаковка и установка

Планируется автономная установка:

- папка программы копируется на компьютер;
- папка добавляется в Support Path или используется загрузчик;
- файл `lcm.cuix` загружается через `_menuload`;
- программа появляется в интерфейсе AutoCAD.

---

## 17. Рабочий процесс с Git и GitHub

### 17.1. Обновление перед началом работы

```bash
git pull origin main
```

---

### 17.2. Сохранение изменений

```bash
git add .
git commit -m "Описание изменений"
git push
```

---

### 17.3. Пример сообщений коммитов

```bash
git commit -m "Этап 2: добавлен lcm_main.lsp и команда LCM"
```

```bash
git commit -m "Этап 3: реализованы чтение точек и отрисовка стрелок"
```

```bash
git commit -m "Исправлен цвет стрелок на ByLayer"
```

```bash
git commit -m "Этап 4: реализован DCL-интерфейс Layer Cycle Manager"
```

```bash
git commit -m "README: добавлены результаты Этапа 4 и описание DCL-интерфейса"
```

---

## 18. Исходный прототип

Исходная программа была написана на Python.

Использовались:

- `pyautocad`;
- `tkinter`;
- `math`;
- `time`;
- опционально `ezdxf`.

Основные файлы прототипа:

- `draw_arrows.py`;
- `arrow_ui.py`;
- `run.py`;
- `description.md`.

В текущей нативной версии AutoCAD логика прототипа переносится в:

- `lcm_data.lsp`;
- `lcm_core.lsp`;
- `lcm_ui.lsp`;
- `lcm_ui.dcl`.

Python в новой версии не используется.

---

## 19. Примечание по кодировке DCL

Файл:

```text
lcm_ui.dcl
```

содержит русские подписи.

Если в AutoCAD русские буквы отображаются некорректно, рекомендуется сохранить именно этот файл в кодировке:

```text
Windows 1251
```

Это можно сделать в VSCode:

1. Открыть `lcm_ui.dcl`.
2. Нажать на кодировку в правом нижнем углу.
3. Выбрать:
   ```text
   Save with Encoding
   ```
4. Выбрать:
   ```text
   Windows 1251
   ```

Для остальных файлов проекта кодировку можно оставлять прежней, если нет проблем с отображением.

---
